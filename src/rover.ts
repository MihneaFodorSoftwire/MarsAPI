export class Rover {
    constructor(
        public id: number,
        public name: string,
        public landing_date: string,
        public launch_date: string,
        public status: string
    ) {}
}